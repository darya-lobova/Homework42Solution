#include <iostream>
#include <string>
#define DEFAULT_SIZE 100

using namespace std;

int find_positive_elements(int array[DEFAULT_SIZE], int length);
int find_negative_elements(int array[DEFAULT_SIZE], int length);
int find_zero_elements(int array[DEFAULT_SIZE], int length);

void print(string s);